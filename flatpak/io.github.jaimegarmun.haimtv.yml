# Generated by flatpak-flutter v0.15.0 from flatpak-flutter.yml.
# Regenerate with flatpak-flutter when dependencies change (see README.md);
# the mpv module (modules/mpv.yml) is added by hand and preserved on regen.
#
# Visit the project at https://github.com/TheAppgineer/flatpak-flutter

id: io.github.jaimegarmun.haimtv
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: haim_tv
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --device=dri
  - --filesystem=xdg-download
  - --filesystem=xdg-videos
  - --talk-name=org.gnome.SessionManager
modules:
  - modules/mpv.yml
  - name: open_tv
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: flutter/build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: flutter/build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin:/var/lib/rustup/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        PUB_CACHE: /run/build/open_tv/.pub-cache
        CC: clang
        CXX: clang++
        CARGO_HOME: /run/build/open_tv/cargo
        RUSTUP_HOME: /var/lib/rustup
    build-commands:
      - cargo build --release --offline
      - install -Dm755 target/release/libfred_tv_lib.so flutter/linux/libs/libfred_tv_lib.so
      - setup-flutter.sh -C flutter
      - (cd flutter && flutter build linux --release --no-pub --verbose)
      - install -Dm755 $BUNDLE_PATH/haim_tv /app/bin/haim_tv
      - cp -r $BUNDLE_PATH/lib /app/bin/lib
      - cp -r $BUNDLE_PATH/data /app/bin/data
      - install -Dm644 io.github.jaimegarmun.haimtv.desktop /app/share/applications/io.github.jaimegarmun.haimtv.desktop
      - install -Dm644 io.github.jaimegarmun.haimtv.metainfo.xml /app/share/metainfo/io.github.jaimegarmun.haimtv.metainfo.xml
      - install -Dm644 flutter/assets/icon.png /app/share/icons/hicolor/512x512/apps/io.github.jaimegarmun.haimtv.png
      # Smaller sizes too: taskbars and docks look up 48/64/128 px icons.
      - for s in 16 24 32 48 64 128 256; do install -Dm644 flatpak/icons/${s}x${s}.png /app/share/icons/hicolor/${s}x${s}/apps/io.github.jaimegarmun.haimtv.png; done
    sources:
      - type: git
        url: https://github.com/jaimegarmun/Haim-TV-IPTV.git
        commit: 5108c47fa1234f16ad2de0b8fc7875b720d7e76e
      - type: file
        path: io.github.jaimegarmun.haimtv.desktop
      - type: file
        path: io.github.jaimegarmun.haimtv.metainfo.xml
      - generated/sources/cargo.json
      - generated/sources/pubspec.json
    modules:
      - generated/modules/rustup-1.94.0.json
      - generated/modules/flutter-sdk-3.44.4.json
